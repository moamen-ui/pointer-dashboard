# Logs
logs
*.log
npm-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Pointer — only config.json and stack.json are committed; pointer.sh, credentials and the AI skill
# files are served by the API and re-installed per clone (`npx pointer-feedback init` joins).
.pointer/*
!.pointer/stack.json
!.pointer/config.json
.claude/skills/pointer-init/
.claude/skills/pointer-feedback/
.agents/pointer-init/
.agents/pointer-feedback/
